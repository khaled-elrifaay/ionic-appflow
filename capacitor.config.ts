import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.appflow.com',
  appName: 'appflow',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
